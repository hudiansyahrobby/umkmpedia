// import React from 'react';
// import Review from './Review';

// export default function Reviews({ reviews }) {
//   return (
//     <div className='mt-6'>
//       {reviews.length > 0 ? (
//         reviews.map((review) => {
//           return <Review review={review} />;
//         })
//       ) : (
//         <h2>This product doesn't have any review yet</h2>
//       )}
//     </div>
//   );
// }
