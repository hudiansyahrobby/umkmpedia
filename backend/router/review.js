// const express = require('express');

// const reviewController = require('../controller/review');
// const { verifyUser } = require('../middleware/userAuth');

// const router = express.Router();

// router.post('/products/:productId/review/add-review', verifyUser, reviewController.addReview);

// router.put('/products/:productId/review/:reviewId', verifyUser, reviewController.updateReview);

// router.delete('/products/:productId/review/:reviewId', verifyUser, reviewController.deleteReview);

// module.exports = router;
